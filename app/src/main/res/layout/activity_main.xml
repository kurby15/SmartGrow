<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <!-- 🟢 1. Dynamic Fragment Container Stack -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation_bar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 🟢 2. Custom Elevation Shadow for Bottom Navigation -->
    <View
        android:id="@+id/view_nav_shadow"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="#E0E0E0"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation_bar"/>

    <!-- 🟢 3. Premium Bottom Navigation Menu Wrapper -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation_bar"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:background="#FFFFFF"
        app:elevation="8dp"
        app:labelVisibilityMode="unlabeled"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/selector_nav_colors"
        app:itemActiveIndicatorStyle="@style/CustomNavIndicator"
        app:itemBackground="?attr/selectableItemBackgroundBorderless"
        app:itemRippleColor="#200C6211"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 🟢 4. Floating Action Chat Circle (Naka-umbok sa Gitna para sa SmartGrow AI Assistant) -->
    <!-- 🛠️ BINAGO: Pinalitan ang ID mula card_nav_camera tungo sa card_nav_chat_assistant -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_nav_chat_assistant"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginBottom="20dp"
        app:cardCornerRadius="32dp"
        app:cardBackgroundColor="#0C6211"
        app:cardElevation="10dp"
        app:strokeWidth="4dp"
        app:strokeColor="#FFFFFF"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 🛠️ BINAGO: Pinalitan ang src mula sa camera icon tungo sa chat bubble icon -->
        <ImageView
            android:id="@+id/img_nav_chat_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_chat_bubble_white"
            android:contentDescription="SmartGrow AI Assistant" />
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>