<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.SmartGrow" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:statusBarColor">#0C6211</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <!-- 🎨 Solusyon 2: Gawing kakulay ng Dashboard background ang Navigation Bar sa ilalim -->
        <item name="android:navigationBarColor">#F9FAFA</item>

        <!-- 👁️ Piliting maging dark/black ang icons (Back, Home, Recent) para litaw sa puting background -->
        <item name="android:windowLightNavigationBar" tools:targetApi="o">true</item>
    </style>

    <style name="Theme.SmartGrow" parent="Base.Theme.SmartGrow" />

    <!-- Navigation Fix: Removes the pill background (indicator) that hides icons on some phones -->
    <style name="CustomNavIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@android:color/transparent</item>
    </style>

    <!-- Custom Loading Style -->
    <style name="CustomLoadingDialog" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>
</resources>